import { GsapPageClient } from "./gsap-page-client";

export const metadata = {
  title: "GSAP Storytelling — The Creative Spark",
  description:
    "A scroll-driven storytelling page built with GSAP ScrollTrigger, featuring Neo-Brutalist design. 6 chapters, 5 scroll behaviors.",
};

export default function GsapPage() {
  return <GsapPageClient />;
}
